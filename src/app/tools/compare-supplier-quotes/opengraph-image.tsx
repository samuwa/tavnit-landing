import { liteOgImage, LITE_OG_SIZE } from "@/components/lite/og";
import { TOOL_COPY } from "@/lib/lite/copy";

export const alt = TOOL_COPY["quote-comparison"].en.title;
export const size = LITE_OG_SIZE;
export const contentType = "image/png";

export default function OpengraphImage() {
  return liteOgImage({
    title: TOOL_COPY["quote-comparison"].en.h1,
    subtitle: "Free. Two or three quotes, one table, the cheapest per line marked.",
    locale: "en",
  });
}
