import LiteHubPage, { liteHubMetadata } from "@/components/lite/LiteHubPage";

export const metadata = liteHubMetadata("en");

export default function ToolsHubPage() {
  return <LiteHubPage locale="en" />;
}
